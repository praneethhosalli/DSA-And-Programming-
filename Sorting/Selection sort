class Solution:
    def selectionSort(self, nums):

        n = len(nums )

        for i in range(0 ,  n ):
            mini = i 

            for j in range(i,n):
                if nums[j]<nums[mini]:
                    mini = j 
            nums[i] , nums[mini ] = nums[mini ] , nums[i]

        return nums 



