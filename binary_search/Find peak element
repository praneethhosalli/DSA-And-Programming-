class Solution:
    def findPeakElement(self, arr):

        if len(arr) == 1 :
            return 0 

        if arr[0]>arr[1]:
            return 0 

        for i in range (1,len(arr)-1):
            if not  arr[i-1]<arr[i]<arr[i+1]:
                return i  

        return len(arr)-1
